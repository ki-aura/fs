From fs Project Folder
===================
grep -n "2.1.0" *.c
git add .
git commit -m "ready for v2.1.0"
git push
git tag -a v2.1.0 -m "Release v2.1.0"
git push origin v2.1.0

From Github
===========
Project will still be under the active 1.3 branch
Create a new release based on 2.1.0 tag
OR USE COMMAND LINE
gh release create v2.1.0 --title "Release v2.1.0" --notes "Release v2.1.0 notes..."
